# Odoo Mates Accounting Compatibility

This is a minimal Odoo 19 compatibility shim for demo templates that declare a
dependency on `om_account_accountant`.

It does not replace the full Odoo Mates accounting package. For production,
install or port the complete Odoo 19 accounting bundle if those features are
required.
