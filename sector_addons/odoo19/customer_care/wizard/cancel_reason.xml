<odoo>
    <record id="cancel_reason_tree_view" model="ir.ui.view">
        <field name="name">cancel.reason.tree.view</field>
        <field name="model">cancel.reason</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="cancel_reason_wizard_form" model="ir.ui.view">
        <field name="name">cancel.reason.wizard.form</field>
        <field name="model">cancel.reason.wizard</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <field name="cancel_reason_id"/>
                    </group>
                </sheet>
                <footer>
                    <button name="confirm" string="Confirm" type="object" class="oe_highlight"/>
                    or
                    <button string="Cancel" class="oe_highlight" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="cancel_reason_action_view" model="ir.actions.act_window">
        <field name="name">Cancel Reasons</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">cancel.reason</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="cancel_contract_reason"
              name="Cancellation Reason"
              parent="customer_care_config"
              action="cancel_reason_action_view" sequence="1"/>
</odoo>