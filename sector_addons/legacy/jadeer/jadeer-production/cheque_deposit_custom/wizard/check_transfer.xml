<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="transfer_check_form" model="ir.ui.view">
        <field name="name">transfer.check.Form</field>
        <field name="model">transfer.check</field>
        <field name="arch" type="xml">
            <form string="Transfer Check">
                <group>
                    <group>
                        <field name="vendor_id" domain="[('supplier_rank','>',0)]" required="1"/>
                        <field name="payment_id" invisible="1"/>
                    </group>
                </group>
                <footer>
                    <button name="apply" string="Transfer" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>